sudo: required
language: java
jdk:
  - openjdk8
services:
  - docker
before_install:
install:
  - mvn clean package sonar:sonar -DskipTests=true -Dsonar.host.url=${SONAR_HOST} -Dsonar.login=${SONAR_TOKEN}
test:
cache:
  directories:
    - $HOME/.m2
script:
branches:
  only:
    - master
